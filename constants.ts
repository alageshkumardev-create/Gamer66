export const GAME_WIDTH = 400;
export const GAME_HEIGHT = 600;

export const BIRD_SIZE = 30;
export const BIRD_LEFT_POSITION = 80;

export const GRAVITY = 800;
export const JUMP_VELOCITY = -350;

export const OBSTACLE_WIDTH = 60;
export const OBSTACLE_MIN_HEIGHT = 50;
export const OBSTACLE_SPAWN_RATE = 1.8; // seconds
export const OBSTACLE_SPEED = -150; // px per second
export const OBSTACLE_GAP_MIN = 180;
export const OBSTACLE_GAP_MAX = 220;

export const BACKGROUND_SPEED = OBSTACLE_SPEED / 3; // Slower for parallax